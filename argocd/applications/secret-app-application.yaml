apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secret-app
  namespace: argocd
spec:
  project: hashi-apps
  source:
    repoURL: https://github.com/midhundha/terraform-aws-eks-argocd-vault.git
    targetRevision: main
    path: apps/secret-app/k8s-manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: secret-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true